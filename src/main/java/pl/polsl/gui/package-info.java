/**
 * The gui package contains classes responsible for the graphical user interface (GUI) components
 * of the application. These classes include MainFrame, UserFrame, AdminFrame, ButtonEditor, and ButtonRenderer.
 * MainFrame serves as the main window of the application, while UserFrame and AdminFrame provide
 * interfaces for regular users and administrators, respectively. ButtonEditor and ButtonRenderer are
 * custom components for handling button editing and rendering in tables.
 *
 * <p>Example usage:
 * <pre>{@code
 *   MainFrame mainFrame = new MainFrame();
 *   UserFrame userFrame = new UserFrame();
 *   AdminFrame adminFrame = new AdminFrame();
 * }</pre>
 *
 * @since 1.0
 */
package pl.polsl.gui;
