/**
 * The model package contains classes representing entities in the application domain.
 * These classes include User, Movie, Genre, and corresponding DAO (Data Access Object) classes.
 * The DAO classes are responsible for interacting with the database to perform CRUD operations.
 *
 * <p>Example usage:
 * <pre>{@code
 *   User user = new User();
 *   Movie movie = new Movie();
 *   Genre genre = new Genre();
 *
 *   // DAO usage
 *   UserDao userDao = new UserDao();
 *   MovieDao movieDao = new MovieDao();
 *   GenreDao genreDao = new GenreDao();
 * }</pre>
 *
 * @since 1.0
 */
package pl.polsl.model;
